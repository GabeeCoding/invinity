import { h } from "preact"

const About = () => (
	<div>
		<h1>About page</h1>
		<p>Testing preact router, so what is this anyway?</p>
		<p>once again, it is music player that uses Invidious API, designed for mobile</p>
	</div>
)

export default About;
